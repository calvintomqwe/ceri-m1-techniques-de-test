version: 2.1

orbs:
  codecov: codecov/codecov@4.0.1  # Utilisation de l'orbe Codecov

jobs:
  build:
    docker:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - run:
          name: Build and Test with Maven
          command: mvn clean verify
      - run:
          name: Generate JaCoCo Report
          command: mvn jacoco:report
      - persist_to_workspace:
          root: target/site
          paths:
            - jacoco

  upload-coverage:
    docker:
      - image: circleci/openjdk:8
    steps:
      - attach_workspace:
          at: /workspace
      - codecov/upload:
          file: /workspace/jacoco/jacoco.xml
          token: CODECOV_TOKEN

workflows:
  version: 2
  build-and-upload:
    jobs:
      - build
      - upload-coverage:
          requires:
            - build
          filters:
            branches:
              only:
                - master
